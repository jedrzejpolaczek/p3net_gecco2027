\begin{abstract}
% TODO: write the abstract. Points to hit, one to two sentences each, in this order
% (adapted from the old planning notes to match the current P3Net design):
%   1. Problem: evaluation-limited joint architecture+hyperparameter search in a discrete
%      space; the accuracy/compute-cost multiobjective trade-off.
%   2. Method: P3Net = P3 (linkage-tree-guided crossover, no mutation) + a relative,
%      linkage-aware surrogate delta-hat_F that predicts the CHANGE in accuracy from a
%      proposed modification, contrasted with NSGANetV2's absolute-regressor surrogate.
%   3. Evaluation design: NSGANetV2 + matched engine/surrogate ablations + SH-EMOA/MO-BOHB,
%      on JAHS-Bench-201 and NAS-HPO-Bench-II, budgets {50,100,200}, R=10 seeds,
%      hypervolume (+ IGD+ where an oracle front is enumerable).
%   4. Headline result(s) — ONLY once Results/Findings is populated; no invented numbers.
%   5. (optional) reproducibility/artifact statement, if one is planned.
% Peer-review must-haves: don't claim anything not backed by a figure/table in Results;
% name the primary metric explicitly.
\end{abstract}

% TODO before submission: CCS concepts are mandatory for papers over two
% pages. The codes below are PLACEHOLDERS (not real ACM taxonomy IDs) —
% generate the real ones at https://dl.acm.org/ccs by searching for concepts
% such as "Computing methodologies~Genetic algorithms", "...~Neural
% networks", and "...~Search methodologies", then paste the tool's output
% here in place of this block.
\begin{CCSXML}
<ccs2012>
<concept>
<concept_id>00000000.00000000.00000000</concept_id>
<concept_desc>Computing methodologies~Genetic algorithms</concept_desc>
<concept_significance>500</concept_significance>
</concept>
<concept>
<concept_id>00000000.00000000.00000000</concept_id>
<concept_desc>Computing methodologies~Neural networks</concept_desc>
<concept_significance>300</concept_significance>
</concept>
</ccs2012>
\end{CCSXML}

\ccsdesc[500]{Computing methodologies~Genetic algorithms}
\ccsdesc[300]{Computing methodologies~Neural networks}

\keywords{neural architecture search, surrogate predictor, P3, evolutionary optimisation, multi objective optimisation}
