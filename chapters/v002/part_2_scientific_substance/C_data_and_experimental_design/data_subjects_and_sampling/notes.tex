\subsection*{Notes for VIII. DATA, SUBJECTS, AND SAMPLING}
\paragraph{Objective}
Describe datasets, splits, preprocessing/augmentation, and any sampling logic. Due to the fact that there are no human subjects, I will treat “subjects” as datasets/tasks.

\paragraph{TODO (Write next):}
\begin{itemize}
  \item Dataset(s): versions, train/val/test split, any fixed seeds used to produce splits.
  \item Preprocessing and augmentation policy (exact operations).
  \item Training recipe constants used for evaluating architectures (optimizer, LR schedule, epochs per rung).
  \item Justify representativeness: what conclusions do/ do not transfer beyond this dataset/search space.
  \item NOTE: one more time explain the impact of choosing data set for testing and why this dataset.
\end{itemize}

\paragraph{Placement note}
If my Experimental Protocol contains these details, then:
\begin{itemize}
  \item keep this section as the canonical dataset description and reference it later, or
  \item merge it into the protocol to avoid duplication.
\end{itemize}

\paragraph{Peer-review must-haves}
\begin{itemize}
  \item Exact evaluator recipe (without it, results are not reproducible).
\end{itemize}
