\subsection*{Notes for X. BENCHMARK DEFINITION}
\paragraph{Objective}
Either consolidate into IX.B / VI or use this as a construct-validity section: why the benchmark meaningfully evaluates your claims.

\paragraph{TODO (Write next):}
\begin{itemize}
  \item Why CIFAR-scale + cell-based search spaces are appropriate for testing your specific claims.
  \item What is not covered (external validity): other datasets, other search spaces (macro), real device latency.
\end{itemize}

\paragraph{Re-organization note}
This section is likely redundant with IX.B and VI; consider merging to avoid repetition.
