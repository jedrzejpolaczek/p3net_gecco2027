\subsection*{Notes for XII. STATISTICAL METHODOLOGY}
\paragraph{Objective}
If kept separately from IX.F, this should define tests formally and ensure consistency with what you run.

\paragraph{TODO (Write next):}
\begin{itemize}
  \item Define the summary metric(s) per-run used for the tests (e.g., HV gap in budget $B$).
  \item Define paired design and why pairing is justified (common random numbers / same budgets).
  \item Define the Holm-Bonferroni procedure and the reporting format.
  \item Define the effect size measurement and interpretation.
  \item Define censoring rules if time-to-target is used.
\end{itemize}

\paragraph{Re-organization note}
This is redundant with IX.F unless you keep IX.F as a short summary and place full details here.
