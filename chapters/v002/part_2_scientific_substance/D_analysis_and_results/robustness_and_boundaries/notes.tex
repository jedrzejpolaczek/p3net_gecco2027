\subsection*{Notes for XIV. ROBUSTNESS AND BOUNDARIES}
\paragraph{Objective}
Explain where the method works, where it fails, and why. Connect limitations with evidence/ diagnosis.

\paragraph{TODO (Write next):}
\begin{itemize}
  \item Robustness across budgets and seeds (show stability, not just best-case).
  \item Sensitivity to key hyperparameters (e.g., $\rho$, archive size $A$, update period $U$).
  \item Boundary conditions: elite scarcity at the top rung, mode collapse risks, proxy mismatch (FLOPs vs real latency).
  \item Threats to validity: construct/internal/external.
\end{itemize}

\paragraph{Peer-review must-haves}
\begin{itemize}
  \item Honest limitations (reduce reviewer skepticism) and evidence-backed robustness checks.
\end{itemize}
