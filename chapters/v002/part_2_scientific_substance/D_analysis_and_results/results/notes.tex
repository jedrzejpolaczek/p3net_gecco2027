\subsection*{Notes for XIII. RESULTS}
\paragraph{Objective}
Present empirical evidence only. No untested claims. Every claim should point to a figure/table.

\paragraph{TODO (Write next - once experiments exist):}
\begin{itemize}
  \item Main convergence plots vs evaluations (median + IQR across runs), across budgets.
  \item Fixed-budget summary table (median/IQR) with corrected p-values and effect sizes.
  \item Multiobjective: Pareto front progression snapshots and HV/HV-gap curves.
  \item Report failure rates and invalid-sample rates (especially important for generative/probabilistic methods).
  \item Report compute accounting summary alongside performance.
\end{itemize}

\paragraph{Peer-review must-haves}
\begin{itemize}
  \item Baseline fairness is demonstrated by consistent budgets, an evaluator, and a tuning policy.
  \item Statistical significance with correction and effect sizes.
\end{itemize}

\paragraph{Suggested figures/tables}
\begin{itemize}
  \item Convergence curves in multiple budgets.
  \item Pareto progression figure.
  \item Results table with corrected stats + effect sizes.
\end{itemize}
