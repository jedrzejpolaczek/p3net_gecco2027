\subsection*{Notes for VII. THEORETICAL FOUNDATIONS}
\paragraph{Purpose}
Add targeted theory only if it supports algorithmic choices or clarifies complexity/overhead; avoid generic theory.

\paragraph{TODO (Write next -pragmatic):}
\begin{itemize}
  \item Complexity per update: mutual information estimation, MST construction, parameter estimation, sampling, and repair.
  \item Overhead discussion relative to training cost (quantify with rough order-of-magnitude, then confirm in execution details).
  \item Optional: a short argument for why using mixed-fidelity elites can bias the learned generator (selection on noisy/low-fidelity metrics).
\end{itemize}

\paragraph{Peer-review must-haves}
\begin{itemize}
  \item If you claim “to be "efficient”, show the overhead scaling and report it empirically.
\end{itemize}
