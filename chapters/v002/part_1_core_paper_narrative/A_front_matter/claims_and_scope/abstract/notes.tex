\subsection*{Notes for ABSTRACT}
\paragraph{Objective}
Summarize the problem setting, the proposed method (P3Net), the evaluation regime (budgets + multi-fidelity), and the key empirical findings \emph{after they exist}. I need to keep it dense and testable/scannable.

\paragraph{TODO:}
\begin{itemize}
  \item 1--2 sentences: evaluation-limited NAS/HPO in a discrete space; multiobjective trade-off (e.g., error vs compute proxy).
  \item 1--2 sentences: what P3Net is (NSGA-II-style Pareto selection + P3 probabilistic generator + ASHA-consistent elite learning + validity/diversity mechanisms).
  \item 1 sentence: summary of experimental design (benchmarks, budgets, repetitions, primary metric).
  \item 1 sentence: headline result(s) once available (no numbers until you have them).
  \item 1 sentence: reproducibility artifact statement (code/config/logs/container).
\end{itemize}

\paragraph{Peer-review must-haves}
\begin{itemize}
  \item Avoid claims that are not directly supported by figures/tables in the Results.
  \item State the primary metric (e.g., HV gap vs evaluations) explicitly.
\end{itemize}
