## What this changes

<!-- One or two sentences: what does this PR do, and why? -->

## Checklist

- [ ] `uv run ruff check .` and `uv run ruff format --check .` pass locally
- [ ] `uv run pytest` passes locally
- [ ] Tests added/updated for any behaviour change (see `TASKS.md`'s
      per-phase test coverage for the expected pattern)
- [ ] If this touches a method's documented simplification (e.g.
      `methods/nsganetv2.py`'s missing continuous-Θ variant), the module
      docstring and `TASKS.md`/`CHANGELOG.md` are updated to match
- [ ] Linked issue (if any):

## Notes for reviewers

<!-- Anything non-obvious: design tradeoffs, things you're unsure about,
     open questions. -->
