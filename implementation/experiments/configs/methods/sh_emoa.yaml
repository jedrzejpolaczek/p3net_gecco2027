# SH-EMOA baseline (multi-objective EA built on SMS-EMOA).
#
# UNVERIFIED AGAINST LIVE DATA: wraps methods/external/sh_emoa.py's
# ask/tell scaffolding with the Stage-B stand-in sampler
# (methods.external._ask_tell_shared.default_valid_sampler) -- the real
# pymoo-based SH-EMOA proposals are not wired up yet (Stage C,
# ../../TASKS.md). Parameters matching guerreroviu2021bagofbaselines's
# original SH-EMOA setup for this joint setting are deferred until the
# real backend replaces the stub sampler.
#
# Reference: chapters/v003/related_work/main.tex ("A systematic comparison
# of solvers on this setting...").

method: sh_emoa
params: {}
