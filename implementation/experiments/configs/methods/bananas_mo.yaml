# Baseline (v0.0.4, plan M2): neural-predictor NAS -- BANANAS with a 5-member MLP ensemble, independent Thompson sampling and mutation-based acquisition over the whole joint genotype (methods/bananas_mo.py).
#
# Hyperparameters follow the original paper's defaults, never tuned on these benchmarks.

method: bananas_mo
params:
  n_initial: 10
  ensemble_size: 5
