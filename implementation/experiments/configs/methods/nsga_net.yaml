# TODO: define the NSGA-Net method config (engine-only ablation, NSGA-II
# side).
#
# Must specify:
#   - search engine: NSGA-II (search_engines/nsga2/), no surrogate
#   - every candidate fully evaluated
#
# Reference: chapters/v003/results/main.tex ("Baselines" -- NSGA-Net
# paragraph).
