# NSGA-Net (engine-only ablation, NSGA-II side): plain NSGA-II, no
# surrogate, every candidate fully evaluated -- symmetric to p3_alone.yaml
# on the P3 side.
#
# Reference: chapters/v003/results/main.tex ("Baselines" -- NSGA-Net
# paragraph).

method: nsga_net
params:
  population_size: 20
  mutation_rate: 0.1
