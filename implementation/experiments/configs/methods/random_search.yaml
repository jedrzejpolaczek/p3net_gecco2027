# TODO: define the random-search sanity-baseline config.
#
# Must specify:
#   - configures experiments/methods/random_search.py (own implementation,
#     not a wrapper -- see methods/external/__init__.py for why this one
#     baseline is not in methods/external/)
#   - uniform sampling over Lambda* under the shared discretised Theta
#     encoding
#
# Reference: chapters/v003/results/main.tex ("Baselines" -- sanity
# baselines).
