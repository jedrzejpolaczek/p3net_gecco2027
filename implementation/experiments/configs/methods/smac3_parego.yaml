# Baseline (v0.0.4, new): SMAC3 random-forest BO with ParEGO, HyperparameterOptimizationFacade defaults (methods/external/smac_parego.py).
#
# Hyperparameters are library or literature defaults, never tuned on these
# benchmarks (paper, Limitations: tuning asymmetry).

method: smac3_parego
params: {}
