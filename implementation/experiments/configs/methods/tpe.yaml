# TODO: define the TPE sanity-baseline config.
#
# Must specify:
#   - wraps methods/external/tpe.py
#   - same discretised joint genotype encoding as every other arm
#
# Reference: chapters/v003/results/main.tex ("Baselines" -- sanity
# baselines).
