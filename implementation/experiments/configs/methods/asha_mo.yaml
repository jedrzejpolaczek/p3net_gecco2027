# Multi-fidelity track (v0.0.4, new): ASHA (Li et al., 2020) with one worker,
# eta = 3, rungs of Hyperband's most aggressive bracket, multi-objective
# promotion as in MO-ASHA (methods/multi_fidelity.py). Budget is in
# full-evaluation equivalents; reported separately from the single-fidelity arms.

method: asha_mo
params: {}
