# TODO: define the P3Net method config.
#
# Must specify:
#   - search engine: P3 (search_engines/p3/)
#   - surrogate: relative, linkage-aware delta_hat_F
#     (surrogates/relative_linkage_aware.py)
#   - chain depth kappa, default = eLyMPuS's 2*ceil(log2(n)) bound, n =
#     full extended discretised genotype dimensionality (architecture +
#     Theta)
#   - step-3 acceptance threshold, default = 0 (delta_hat_F >= 0)
#   - fidelity scope: r_K only
#
# Reference: chapters/v003/proposed_optimizer/main.tex (entire section);
# chapters/v003/results/main.tex ("Baselines", Table tab:ablation-grid).
