# TODO: define the P3 + absolute-regressor method config (surrogate-only
# ablation).
#
# Must specify:
#   - search engine: P3 (search_engines/p3/)
#   - surrogate: absolute regressor (surrogates/absolute_regressor.py),
#     NSGANetV2 style -- NOT delta_hat_F
#   - acceptance rule adapted for an absolute (not relative) surrogate
#     prediction
#
# Reference: chapters/v003/results/main.tex ("Baselines" -- "P3 with an
# absolute regressor surrogate...").
