# Multi-fidelity track (v0.0.4, new): Hyperband (Li et al., 2018), eta = 3,
# random sampling, multi-objective promotion by non-dominated rank + crowding
# distance (methods/multi_fidelity.py). Budget is in full-evaluation
# equivalents; reported separately from the single-fidelity arms.

method: hyperband_mo
params: {}
