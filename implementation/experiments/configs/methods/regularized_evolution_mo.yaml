# Baseline (v0.0.4, new): Regularized (aging) evolution adapted to two objectives (methods/regularized_evolution_mo.py). population_size 20 matches every other population-based arm; sample_size 5 keeps Real et al.'s sample/population ratio range.
#
# Hyperparameters are library or literature defaults, never tuned on these
# benchmarks (paper, Limitations: tuning asymmetry).

method: regularized_evolution_mo
params:
  population_size: 20
  sample_size: 5
