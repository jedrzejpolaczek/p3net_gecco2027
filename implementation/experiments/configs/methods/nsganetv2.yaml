# TODO: define the NSGANetV2 method config (primary baseline).
#
# Must specify:
#   - search engine: NSGA-II (search_engines/nsga2/)
#   - surrogate: absolute regressor (surrogates/absolute_regressor.py)
#   - Theta encoding: discretised, IDENTICAL to P3Net's discretisation
#     (Fairness controls) -- not NSGANetV2's native continuous encoding
#     (see nsganetv2_continuous.yaml for that control)
#
# Reference: chapters/v003/results/main.tex ("Baselines", "Fairness
# controls").
