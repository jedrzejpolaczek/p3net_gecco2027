# Baseline (v0.0.4, plan M1): RL-based NAS -- REINFORCE with a factorised categorical policy over the whole joint genotype, random-weight Chebyshev reward (methods/reinforce_mo.py).
#
# Hyperparameters are this project's fixed defaults, never tuned on these benchmarks.

method: reinforce_mo
params:
  learning_rate: 0.1
  baseline_momentum: 0.9
