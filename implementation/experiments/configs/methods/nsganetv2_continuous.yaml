# TODO: define the NSGANetV2-with-continuous-Theta control config.
#
# Must specify:
#   - same as nsganetv2.yaml, EXCEPT Theta uses its native unconstrained
#     real-valued encoding (not the discretised encoding shared by every
#     other arm)
#   - purpose: isolate the effect of discretisation itself from the
#     effect of the search engine / surrogate
#
# Reference: chapters/v003/results/main.tex ("Baselines" -- "NSGANetV2
# extended to the joint setting with an unconstrained, real-valued
# encoding of Theta as an additional control baseline").
