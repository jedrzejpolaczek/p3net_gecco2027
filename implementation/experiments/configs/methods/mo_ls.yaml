# Baseline (v0.0.4, new): Multi-objective local search: Pareto Local Search (methods/mo_ls.py). No hyperparameters.
#
# Hyperparameters are library or literature defaults, never tuned on these
# benchmarks (paper, Limitations: tuning asymmetry).

method: mo_ls
params: {}
