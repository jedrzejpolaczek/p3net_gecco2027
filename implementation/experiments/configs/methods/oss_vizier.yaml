# Baseline (v0.0.4, new): OSS Vizier's default designer, GP-UCB-PE with native
# multi-objective support (methods/external/vizier_mo.py). Stands in for the
# Vertex AI Vizier service, which cannot be run offline or reproducibly.
#
# Hyperparameters are library defaults, never tuned on these benchmarks
# (paper, Limitations: tuning asymmetry).

method: oss_vizier
params: {}
