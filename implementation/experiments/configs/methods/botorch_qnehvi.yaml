# Baseline (v0.0.4, new): GP Bayesian optimisation, BoTorch qLogNEHVI (methods/external/botorch_mo.py).
#
# Hyperparameters are library or literature defaults, never tuned on these
# benchmarks (paper, Limitations: tuning asymmetry).

method: botorch_qnehvi
params: {}
