# Multi-fidelity track (v0.0.4, new): BOHB (Falkner et al., 2018) -- Hyperband's
# schedule with hpbandster's real BOHB config generator at every rung's epoch
# count; promotion identical to hyperband_mo (methods/multi_fidelity.py).
# Differs from the single-fidelity mo_bohb arm, which always queries full
# training. Budget is in full-evaluation equivalents.

method: bohb_mo
params: {}
