# TODO: define the NAS-HPO-Bench-II search-space config.
#
# Must specify:
#   - the fixed cell topology architecture dimensions
#   - the learning-rate and batch-size hyperparameter dimensions and their
#     discretisation
#   - r_K fixed at the tabulated range only (up to 12 training epochs) --
#     must NOT reference the benchmark's separate 200-epoch GIN+MLP
#     surrogate extrapolation
#
# Reference: chapters/v003/related_work/main.tex ("Classifying
# NAS-HPO-Bench-II as Category 1 assumes that P3Net queries only its
# exhaustively tabulated range...").
