# TODO: define the kappa / acceptance-threshold sensitivity sweep grid.
#
# Must specify:
#   - kappa grid: {1, ceil(log2(n)), 2*ceil(log2(n)), infinity}
#   - acceptance-threshold grid: {0, epsilon, 2*epsilon} for a fixed
#     epsilon > 0
#   - swept JOINTLY (full cross product), P3Net otherwise unchanged
#   - metrics to report as a function of both: fixed-budget hypervolume,
#     surrogate rank correlation
#
# Reference: chapters/v003/results/main.tex ("Surrogate error accumulation
# (kappa and acceptance threshold)").
