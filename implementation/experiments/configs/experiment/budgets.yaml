# TODO: define the full-evaluation budget tiers and run count.
#
# Must specify:
#   - three tiers: {50, 100, 200} full evaluations
#   - R = 10 independent runs per method, fixed published seed list
#   - stopping rule: budget exhaustion OR exploration-collapse criterion
#     (for search spaces small enough to converge prematurely)
#
# Reference: chapters/v003/results/main.tex ("Budgets, seeds, stopping").
